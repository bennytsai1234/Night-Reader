# 2026-09-13

- P1 accepted: established the Reader V2 diagnostics baseline in commit `466ddaed`, restored the two unvalidated production performance changes, and verified `flutter analyze`, 207 Reader V2 tests, and 1028 full-suite tests.
- P2 accepted: added vsync-paced continuous measurement, driver/app TimelineSummary cross-validation, fail-closed invalid guardrails, and valid 120Hz artifacts; the current performance result is valid but below the strict 8ms P99 target and is handed to P4.
- P3 accepted: added the debug-only I1–I8 frame invariant hook and bounded watchdog, fixed progress publication and ballistic chapter-jump restore races, verified three post-fix 120Hz seeds with zero production violations plus a passing journey scenario, and handed renderer/raster performance decisions to P4.
- P4 accepted: kept `RenderCachedBlock.isRepaintBoundary=false` after a valid improvement over baseline, reverted renderer and paint-path experiments without evidence of benefit, recorded a valid lazy-control emulator raster-ceiling attribution, and documented the PowerShell 5.1 `ArgumentList` pitfall with the required `pwsh -NoProfile` runner path. Strict Reader `totalSpan P99 < 8000µs` remains failed; no pass was claimed.
- P4G accepted: added fixed operation-category markers and offline-first attribution, recorded P50/P95/P99/worst plus over-budget-frame and streak evidence in the shared ledger, and separated valid scroll/control/menu failures from invalid or unverified page/TTS/navigation/entry paths. Raster cache export was insufficient for full overdraw/saveLayer attribution, real-device validation remains outstanding, and the unchanged 8ms gate remains failed for every valid measured category; P4 history was not reopened. P4O is required by the conditional trigger.
- P4V accepted with rollback: independent in-app settled screenshot capture found a reproducible candidate-only TTS正文 blank at seed `9145004` (312,976 differing pixels, ratio `0.08561362`), satisfying the explicit revert policy. `RenderCachedBlock.isRepaintBoundary` is restored to `true`; deterministic hook-on state converged and paired performance showed no threshold regression, but unverified Android coverage remains documented. All failure artifacts and revert conditions are preserved; P4 history remains closed.
- P4O accepted: the P4G >8ms trigger was met; H1 adjacent-chapter prelayout was tested with two valid seeds, reduced task P99 but did not improve frame/raster P99, and was reverted. H2–H5 were explicitly not executed without valid entry/navigation or per-frame causal boundaries. Residual raster/overall frame tail remains, the strict 8ms gate is still failed, and the P4V rollback state `isRepaintBoundary=true` is preserved.
- P5 accepted: durable 120Hz execution rules, PowerShell 7 runner compatibility, P3/P4 Reader structure facts, and fail-closed guardrail coverage are now documented in `DEVELOPMENT.md`, `docs/night_reader/reader.md`, and the ledger without duplicating batch raw evidence. No production behavior changed; real-device, navigation/entry, TTS callback, and fine-grained compositing evidence remain explicitly unverified.
- P6 accepted: completed two finite 28-action seeded monkey gates with zero production I1–I8 violations, retained one harness-only I7 observation, fixed the observed settled `textColor` ParagraphCache freshness bug with pre-fix regression evidence, and corrected cross-epoch I8 attribution. Journey and full tests pass; the valid profile continuous result still fails the unchanged strict `<8000µs` P99 gate, and the P4V rollback state `isRepaintBoundary=true` remains authoritative. Font-family, rotation, second-source, real-device/TTS, and fine-grained raster attribution remain explicitly unverified.
- T1 accepted: established shared state-transition test support with semantic anchor, generation/epoch/metrics assertions, a controllable test-only source-switch fake, and four bounded smoke paths for style, viewport/inset, simplified/traditional reload, and source switching. Normal app behavior and the P4V rollback remain unchanged; Android rotation/inset animation and failure-race coverage are deliberately handed to T3–T5.
- T2 accepted: verified seven style dimensions, clamp and locked/unlocked `cellWidth` boundaries, composition, rapid changes, and unsettled-scroll restore; fixed same-frame presentation transition storms and the `lineHeight` spec/signature clamp mismatch with pre-fix regression evidence. `bold` and `fontFamily` remain explicit dead-input skips, and P4V rollback/history remain untouched.
- T3 accepted: confirmed the cross-frame rotation/viewport storm (`12` frames caused `12` presentation dispatches), coalesced it to one latest-spec dispatch with final-size delivery, and verified five portrait/landscape round trips, unsettled rotation, chapter boundaries, and very-short-chapter exact-anchor preservation. Evidence is widget-level only; real-device rotation remains unverified.
- T4 accepted: measured the real `1→2` conversion length expansion in `西游记.txt`, added sentence-aware UTF-16 location remapping, and verified content, measurement, paragraph, and disk-cache freshness plus repeated/edge/unsettled conversion paths. The bounded emulator run was correctly `INVALID` for insufficient/inconsistent frames; no performance or real-device claim was made.
- T5 accepted: fixed page-layer source-switch ordering so the `flushProgress()` snapshot is the single authority for migrated location, and verified static/moving flush races, resolve/persist failures, late dispose flush, short delay, return competition, DB rows, and event/error behavior. Core service tests remained unchanged and green; no network or Android claim was made.
- T6 accepted: captured the four durable Reader V2 state-transition trigger chains, the `textColor`/font-family/bold/source-session contracts, the remaining page-layer source-switch boundary, and the T1–T5 verification entry points in `docs/night_reader/reader.md` and `DEVELOPMENT.md`. Reader V2 subtree verification passed 260 tests with clean analysis; no production code, P4 history, or P4V rollback state changed, and full-suite/Android/performance evidence remains outside this documentation package.
- C1 accepted: established the deterministic 121-chapter / 633-paragraph correctness fixture, independent 19-row ink-profile decode, controlled chapter-content seam, measured `426.6667×952` host viewport, shared vsync-paced operations and case-id contract. Host C1 tests passed 9, fixture SHA-256 was stable, and exact `emulator-5556` journey plus 50/50 Android pixel decode passed; cold setup/open/settle averaged 531.859ms, so C5 must reuse a mounted harness. Oracle/sweep/golden/performance claims remain for C2–C7.
- C2 accepted: extended the frame record with scroll geometry/activity/velocity, queue, token, chapter and error observability; added 14 runtime invariants with 28 positive/negative injection proofs, preserved I8/I1–I7 semantics, and verified real host `drag → ballistic → idle` plus jump token/queue transitions. The C2 target passed 30 tests and the Reader V2 subtree 300; no Android, temporal, visual, sweep, golden, or performance-pass claim was made.
- C3 accepted: added the independent bounded 240-frame temporal oracle with T1–T10 positive/negative sequence proofs, window JSON, runtime/temporal separation, P3 starvation reconstruction, and one-frame `TRANSIENT` retention. Host and bounded Android correctness observations were clean within the 300-second limit; the Android P99 remained `40000µs`/insufficient, so no performance pass or C4–C7 claim was made.
- C4 accepted: added the independent in-process pixel oracle with bounded `RepaintBoundary.toImage()` capture, V1–V20 analyzer proofs, three host Reader injections, C1 profile decoding `50/50`, and bounded Android evidence. Relay independently passed the C4 suite `35`, Reader V2 `356`, and full repository `1178`; Android action `V1=5/V11=9` remains unclassified for C5, capture coverage is below 100%, and compositor/SurfaceFlinger plus real-device behavior remain outside the claim. The P4V rollback state `isRepaintBoundary=true` and the strict performance gate are unchanged.
- C5 accepted: added 36-operation deterministic coverage, 4,308-case manifests and bounded host full sweeps. Relay independently reran both seeds with runtime/temporal/visual/cross counts `0/0/0/0`, verified the 240-case fast lane under one second, and preserved the synthetic visual lane's `visualCoverage=0.0` boundary. The preflight `I15/I16` top-boundary signal was correctly fixed as a harness clamp with before/after evidence; C4 Android `V1/V11` remains provisional for C6, with no Android-raster, real-device, 120Hz, or performance claim.
